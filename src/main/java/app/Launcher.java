package app;

/**
 * Non-JavaFX entry point — bypasses the module-path check
 * so the app runs from IntelliJ without VM options.
 */
public class Launcher {
    public static void main(String[] args) {
        Main.main(args);
    }
}
